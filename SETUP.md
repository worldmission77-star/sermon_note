# 설교노트 앱 — 개인용(로컬) 버전

서버·로그인 없이 완전히 개인용으로 바뀌었습니다.

## 어떻게 동작하나요
- 로그인 없이 바로 사용
- 등록한 설교 정보 + 작성한 노트는 **이 브라우저(폰/PC)의 localStorage**에만 저장됩니다
- 인터넷 연결 없이도 (성경 검색 포함) 그대로 사용 가능
- 다른 기기와는 동기화되지 않아요 — 같은 폰/브라우저에서 계속 쓰면 기록이 계속 쌓여요

## 배포
QT 앱과 동일하게, 이 폴더 전체를 Netlify 등에 올리면 됩니다.
- `index.html`
- `manifest.json`
- `sw.js`
- `bible-data.js`
- `icon-192.png`, `icon-512.png`

## 주의할 점
- **브라우저 저장공간(사이트 데이터/캐시)을 지우면 기록도 함께 사라져요.** 사파리/크롬의 "방문 기록 삭제"에서 "쿠키 및 사이트 데이터"를 지우지 않도록 주의해주세요.
- 폰을 바꾸거나 브라우저를 바꾸면 기존 기록은 보이지 않아요. (나중에 "내보내기/가져오기" 기능을 추가하면 이 문제를 해결할 수 있어요 — 필요하시면 말씀해주세요.)
- PWA로 "홈 화면에 추가"해두고 쓰시는 걸 추천드려요. 브라우저 앱을 지우지 않는 한 데이터가 유지됩니다.
